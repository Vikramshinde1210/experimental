spring.application.name=bloom-filter
spring.datasource.url=jdbc:h2:file:./data/usersdb
spring.jpa.hibernate.ddl-auto=update

redis.url=redis://localhost:6379

# Bloom filter: expected false-positive rate and initial capacity
bloom.error-rate=0.01
bloom.capacity=10000
